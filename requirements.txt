numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
pytest>=7.4

# Not required for the synthetic pipeline below, but needed once a real
# VLM is wired in via src/vlm_trojan_cue/model_interface.py:
# transformers>=4.40
# torch>=2.1
# pillow>=10.0
